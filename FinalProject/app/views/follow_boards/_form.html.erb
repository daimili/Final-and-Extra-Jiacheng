<%= form_for(@follow_board) do |f| %>
  <% if @follow_board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@follow_board.errors.count, "error") %> prohibited this follow_board from being saved:</h2>

      <ul>
      <% @follow_board.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :board_id %><br />
    <%= f.number_field :board_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
